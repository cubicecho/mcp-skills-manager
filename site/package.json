{
  "name": "mcp-skills-manager-site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Landing / docs site for MCP Skills Manager (built with Eleventy, hosted on GitHub Pages)",
  "scripts": {
    "dev": "eleventy --serve",
    "build": "eleventy",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
